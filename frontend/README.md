# Frontend Dashboard

This frontend is a React + Vite app for the LCOC MOAA legislative dashboard.

## Commands

- `npm install`
- `npm run dev` — start the development server
- `npm run build` — produce a production build
- `npm run preview` — preview the production build

## Notes

The app is configured to proxy `/api` requests to `http://localhost:4000` during development.
