# Infra

This folder contains local Docker Compose setup for development.

## Run locally

```powershell
cd infra
docker compose up --build
```

The frontend is served on `http://localhost:5173`, and the backend is available on `http://localhost:4000`.
